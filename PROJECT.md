# 小说世界 D&D —— 基于时空记忆驱动的沉浸式小说角色扮演系统

> 个人项目 | 2026.05 | Python / FastAPI / LangGraph / LightRAG / Pydantic v2

## 一、项目简介

将任意小说文本转化为可交互的 RPG 世界。玩家穿越进小说，夺舍已有角色或创建自定义身份，与 NPC 进行对话式交互。系统通过**四层时空记忆架构**严格锁定每个角色在特定剧情节点的认知边界，从根源杜绝"角色知道未来剧情"、"人设崩塌"、"认知超纲"等问题。

**一句话概括**：让大模型扮演小说角色时，角色的每一句话都严格受限于"它在这个时间点应该知道什么"。

---

## 二、核心特点

### 特点 1：四层时空记忆架构

不是简单的"把角色信息塞进 prompt"，而是设计了一套完整的记忆分层体系：

| 层级 | 存储内容 | 读写权限 | 生命周期 |
|------|----------|----------|----------|
| L1 长期记忆 | 角色人设、性格、背景 | 只读 | 全小说 |
| L2 中期记忆 | 已解锁的能力、信息、关系 | 仅追加 | 章节级 |
| L3 短期记忆 | 当前场景的对话、事件 | 读写 | 场景级 |
| L4 工作记忆 | 本轮决策上下文 | 临时 | 单回合 |

每一条记忆都绑定时空坐标 `{小说ID}-{章节}-{场景}-{事件}`，检索时先做时空硬过滤，再做语义召回。角色在第 3 章绝对检索不到第 5 章才解锁的信息。

### 特点 2：LangGraph 决策 DAG

用 LangGraph 构建了角色决策的有向无环图，6 个节点强制流转：

```
记忆召回 → 认知校验 → 决策生成 → 合规校验 → 格式输出 → 记忆更新
                                      ↑            |
                                      └── 失败重试 ──┘ (最多2次)
```

每个节点都是独立的 async 函数，可单独测试、替换、扩展。

### 特点 3：记忆增强的多 Agent 协调

场景中的每个 NPC 都有独立的记忆上下文。协调器在生成前为每个 NPC 召回其专属记忆，注入到统一的 system prompt 中，一次 LLM 调用生成旁白 + 所有 NPC 对话。

---

## 三、难点与解决方案

### 难点 1：如何让大模型"不知道未来的事"？

**问题本质**：大模型预训练时已经读过整本小说，你让它扮演第 3 章的叶凡，它却可能说出第 200 章才知道的信息。prompt 里写"请不要剧透"是不可靠的。

**解决方案：时空坐标硬隔离**

```
检索请求: role_id=叶凡, current_coord=zhetian-0003-0001-0000

SQL 层面:
  WHERE valid_time_coord <= 'zhetian-0003-0001-0000'
    AND expire_time_coord >= 'zhetian-0003-0001-0000'

→ 第 5 章的记忆根本不会出现在检索结果中
→ 大模型看不到未来信息，自然无法说出
```

不是靠 prompt 约束，而是从**数据层面**把未来信息物理隔离。坐标采用零填充字符串格式，支持字典序比较，无需额外数据库。

### 难点 2：如何平衡"记忆准确性"与"LLM 调用延迟"？

**问题本质**：最初设计的 per-NPC Agent 方案，3 个 NPC 场景需要 10+ 次 LLM 调用（每个 NPC 独立跑 pre_validate → generate → post_validate），延迟不可接受。

**解决方案：分层优化策略**

| 优化手段 | 效果 |
|----------|------|
| 去掉 per-NPC 重复校验，改为一次全局校验 | -N 次调用 |
| 旁白+所有 NPC 对话合并为一次生成 | -N 次调用 |
| 记忆召回和写入不走 LLM，纯数据操作 | 0 额外开销 |

最终：**2 次 LLM 调用**（1 校验 + 1 生成），与无记忆系统的 legacy 路径相同，但生成质量显著提升（system prompt 中注入了每个角色的专属记忆上下文）。

**思考过程**：最初信奉"每个 NPC 都是独立 Agent"的理念，实测发现 3 NPC 场景延迟 15s+。分析调用链后发现 pre_validate 和 post_validate 对每个 NPC 重复执行相同逻辑，且 narrator 和 NPC 的生成可以合并。保留了 LangGraph DAG 作为可插拔的高级模式，但默认路径走优化后的单次生成。

### 难点 3：如何处理"信息差"——角色之间的认知不对称？

**问题本质**：小说中角色 A 知道的秘密，角色 B 不知道。如果玩家同时和 A、B 对话，系统必须让 A 和 B 的回复体现这种信息差。

**解决方案：私有/共享记忆隔离**

```python
# 私有记忆：只有角色自己能检索
MemoryFragment(memory_private=True, role_id="韩飞羽", content="我是灵墟洞天弟子")

# 共享记忆：同场景所有角色可检索
MemoryFragment(memory_private=False, content="叶凡问了一个关于修仙的问题")
```

检索时的过滤逻辑：

```python
if frag.memory_private and frag.role_id != request.role_id:
    return False  # 其他角色的私有记忆不可见
```

这让韩飞羽可以隐瞒身份，而叶凡对修仙世界一无所知——完全符合原著设定。

### 难点 4：如何在 JSON 文件上实现"向量检索 + 元数据过滤"？

**问题本质**：没有 PostgreSQL/Milvus，只有 JSON 文件和 numpy，如何实现带时空过滤的语义检索？

**解决方案：两阶段检索**

```
阶段 1: 时空硬过滤 (纯 Python 字符串比较, O(N))
  → 过滤掉所有未来时空的记忆
  → 过滤掉其他角色的私有记忆
  → 剩余: 当前时空下该角色可访问的记忆池

阶段 2: 语义排序 (numpy 余弦相似度, O(N×D))
  → 对记忆池中每条记忆的 embedding 与 query embedding 做点积
  → 按相似度 + 层级优先级 + 时间近度综合排序
  → 返回 Top-K
```

性能足够：单个小说几百条记忆片段，numpy 矩阵运算在毫秒级完成。

### 难点 5：如何保证"向后兼容"——不破坏已有的游戏体验？

**问题本质**：新系统上线后，已上传的小说没有记忆数据，不能直接切换到新路径。

**解决方案：自动路由 + 渐进迁移**

```python
# chat.py 中的路由逻辑
memory_manager = MemoryManager(novel_id)
if memory_manager.has_memory_data():
    # 走记忆增强路径
    return await process_with_multi_agent(...)
else:
    # 回退到传统 GameMaster 路径
    return await gm.process_player_action(...)
```

- 新上传的小说：提取管线末尾自动调用 `migrate_novel_to_memory()`，无需手动操作
- 已有小说：通过 API `POST /novel/{id}/memory/migrate` 触发迁移
- 传统路径完全保留，零改动

---

## 四、技术架构

```
┌─────────────────────────────────────────────────────┐
│                    FastAPI 服务                       │
├──────────┬──────────┬──────────┬────────────────────┤
│ 游戏接口  │ 小说接口  │ 记忆接口  │    前端 (SPA)      │
├──────────┴──────────┴──────────┴────────────────────┤
│              chat.py (会话管理 + 路由)                 │
├─────────────────────┬───────────────────────────────┤
│   MultiAgent        │   GameMaster (legacy)          │
│   Coordinator       │   (无记忆数据时回退)              │
├─────────────────────┴───────────────────────────────┤
│              MemoryManager (记忆管理)                  │
├──────────┬──────────┬──────────┬────────────────────┤
│ L1 人设   │ L2 状态   │ L3 交互   │ L4 工作记忆        │
│ (JSON)   │ (JSON)   │ (内存)    │ (内存)             │
├──────────┴──────────┴──────────┴────────────────────┤
│         MemoryStore (时空过滤 + 向量检索)              │
├─────────────────────────────────────────────────────┤
│         TimeCoord (时空坐标体系)                       │
├──────────┬──────────────────────────────────────────┤
│ LightRAG │  LLM API (MiMo) + Embedding API (BGE-M3) │
└──────────┴──────────────────────────────────────────┘
```

---

## 五、关键数据

| 指标 | 数值 |
|------|------|
| 单次对话 LLM 调用 | 2 次（校验 + 生成） |
| 记忆检索延迟 | < 10ms（numpy 矩阵运算） |
| 单小说记忆片段数 | ~230（10 角色 × 5 章节） |
| 向量维度 | 1024（BGE-M3） |
| 前端兼容性 | 零改动，输出格式不变 |

---

## 六、技术选型思考

| 决策 | 选择 | 原因 |
|------|------|------|
| Agent 框架 | LangGraph | 原生支持有向无环图、条件分支、状态管理，比 CrewAI 更灵活 |
| 向量检索 | numpy | 单小说几百条记忆，不需要 Milvus 的分布式能力，减少运维复杂度 |
| 存储 | JSON 文件 | MVP 阶段足够，接口层已抽象，可随时替换为 PostgreSQL |
| 知识抽取 | LightRAG | 比 GraphRAG 轻量，中文支持好，单文件即可运行 |
| Prompt 策略 | 时空注入 | 不靠"请不要剧透"的软约束，而是从数据层物理隔离未来信息 |

---

## 七、可扩展方向

1. **Neo4j 图数据库**：L1 长期记忆迁移到图数据库，支持复杂关系查询（"叶凡认识的人中，谁认识韩飞羽？"）
2. **PostgreSQL + pgvector**：L2 中期记忆持久化到关系数据库，支持百万级记忆片段的高效检索
3. **per-NPC 独立 Agent**：保留 LangGraph DAG 作为高级模式，用于需要 NPC 之间复杂博弈的场景
4. **记忆衰减与遗忘**：引入时间衰减函数，越久远的记忆权重越低，模拟真实认知
5. **玩家记忆系统**：为穿越者玩家也建立记忆层，记录其在小说世界中的所有经历

---

## 八、项目产出

- 完整可运行的 Web 应用（前端 + 后端）
- 四层时空记忆系统（10 个新模块）
- LangGraph Agent 决策框架（可插拔）
- 数据迁移工具（已有小说一键迁移）
- 15 个 API 端点（游戏 + 记忆管理）
